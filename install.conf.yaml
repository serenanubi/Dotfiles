- clean: ['~', '~/.config']

- link: 
    ~/.gitconfig: gitconfig 
    ~/.gitignore_global: gitignore_global
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.zsh: zsh/
    ~/.zshrc: zshrc
  
- shell:
    - [git update-submodules, Installing/updating submodules]
